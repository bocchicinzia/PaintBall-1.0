export class Sponsor {
  label: string;
  path: string;
  img: string;

  constructor( label: string, path: string, img: string ) {
    this.label = label;
    this.path = path;
    this.img = img;
  }
}
